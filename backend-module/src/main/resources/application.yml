spring:
  application:
    name: beneficios-api
  
  web:
    cors:
      allowed-origins: "*"
      allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"
      allowed-headers: "*"
  
  # Desabilitar JPA - toda lógica de persistência fica no EJB
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

server:
  port: 8080

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha

logging:
  level:
    com.beneficio.backend: DEBUG
    com.beneficio.ejb: DEBUG
    org.springframework.web: DEBUG
    org.hibernate: DEBUG
    org.springframework.orm: DEBUG
